# لوحة تحكم Spectre — النسخة المدمجة (سيرفر واحد بس!)

بشرى: الداشبورد الحين **جزء من نفس البوت** — ما تحتاج سيرفر ثانٍ ولا تربط
بين خادمين ولا أي تعقيد شبكة. لما تشغّل `bot.py`، الداشبورد تشتغل تلقائياً
معه بنفس المنفذ.

## 1) جهّز تطبيق OAuth2 بديسكورد

1. https://discord.com/developers/applications → اختر تطبيق البوت نفسه.
2. **OAuth2 → General** → انسخ **Client ID** و **Client Secret**.
3. تحت **Redirects** أضف رابط سيرفرك + `/callback`، مثال:
   `http://78.154.103.53:9181/callback` (بدّل بعنوانك الحقيقي).

## 2) أضف بملف `.env` (نفس ملف البوت بالضبط)

```
DISCORD_CLIENT_ID=...
DISCORD_CLIENT_SECRET=...
DASHBOARD_SECRET_KEY=نص_عشوائي_طويل_تختاره_أنت
DASHBOARD_REDIRECT_URI=http://عنوان_سيرفرك:البورت/callback
```

لا تحتاج أبداً: `DASHBOARD_API_SECRET` ولا `BOT_API_BASE` ولا `DASHBOARD_PORT`
— هذي كانت للتصميم القديم (خادمين منفصلين)، وألغيناها تماماً.

## 3) أعد تشغيل البوت

نفس أمر التشغيل المعتاد: `python3 bot.py`. راقب الـConsole، لازم تشوف:
```
[Spectre] لوحة التحكم المدمجة تعمل على المنفذ ...
```

## 4) افتح الداشبورد

`http://عنوان_سيرفرك:البورت` بأي متصفح، وسجّل دخول بحساب ديسكورد اللي
عندك فيه صلاحية "إدارة السيرفر".

---

## بديل: تشغيلها منفصلة تماماً (لمن يحتاج المرونة)

لو تبي تفصل الداشبورد لاستضافة أخرى (مثلاً Render) عن البوت لأي سبب، هذا
ممكن لكن يحتاج ربطاً يدوياً بين الخادمين (ملفات `dashboard_api.py` القديمة
لسا موجودة بالمشروع لهذا الغرض). التصميم المدمج هو الافتراضي والموصى به
لأي استضافة بمنفذ خارجي واحد (مثل Wispbyte).


### رابط HTTPS مجاني (اختياري)
إذا أردت رابطاً عاماً آمناً مثل `https://spectre-dashboard-xxxx.onrender.com` بدون شراء دومين، يمكن نشر نسخة الداشبورد الخفيفة على Render Free. رابط `onrender.com` مجاني ولا يضيف شعاراً داخل اللوحة، لكنه يظل تابعاً لنطاق Render، كما أن الخدمة المجانية تتوقف بعد 15 دقيقة من عدم وجود طلبات وتستيقظ عند زيارة الرابط. لا تضع توكن البوت في GitHub؛ ضعه في Environment Variables.
