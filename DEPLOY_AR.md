# نشر بوت Spectre على خدمة مجانية

هذه الحزمة تشغّل `bot.py` الحقيقي مع `db.py` ومتطلبات `discord.py`. لا يوجد أي توكن داخل الملفات. عند إنشاء الخدمة، أضف المتغير السري باسم `DISCORD_TOKEN` من لوحة الخدمة نفسها.

## Render Free

اربط مستودع GitHub الذي يحتوي هذه الملفات، واختر ملف `render.yaml` أو أنشئ Web Service بخ runtime Python. استخدم أمر البناء `pip install -r requirements.txt` وأمر التشغيل `python bot.py`، ثم أضف `DISCORD_TOKEN` كمتغير سري.

الخطة المجانية مناسبة للاختبار، لكنها ليست ضماناً لتشغيل البوت 24/7؛ الخدمة المجانية قد تدخل في وضع السكون بعد 15 دقيقة بلا طلبات واردة، وقد تستغرق قرابة دقيقة عند الاستيقاظ، كما يمكن إعادة تشغيلها أو إيقافها عند استهلاك الساعات المجانية. لذلك لا ينبغي اعتبارها استضافة إنتاجية دائمة [1].

## Wispbyte (مجاني 24/7، يعتمد على Pterodactyl)

1. أنشئ حساباً على wispbyte.com، ثم أنشئ سيرفراً جديداً واختر صورة **Python**.
2. ارفع كل ملفات هذه الحزمة عبر مدير الملفات بلوحة التحكم (تأكد من رفع `requirements.txt` مع الملفات).
3. **Startup Command**: `python3 bot.py` — كافٍ وحده، لأن `bot.py` أصبح الآن "مُشغِّل ذكي" يتحقق تلقائياً من أي اعتمادية بايثون ناقصة ويثبّتها بنفسه (لا تحتاج خطوة Build منفصلة)، ويجهّز FFmpeg تلقائياً من الأرشيف المرفق لو لم يكن مثبتاً بالنظام أصلاً.
4. أضف متغير البيئة `DISCORD_TOKEN` من تبويب **Startup/Variables** بلوحة السيرفر (لا تكتبه أبداً داخل أي ملف).
5. **مهم لاستمرارية البيانات**: قاعدة البيانات (`bot_data.sqlite3`) تُنشأ داخل مجلد السيرفر نفسه — طالما لم تحذف السيرفر بالكامل من Wispbyte، بياناتك (الإعدادات، الخبرة، التذاكر...) تبقى محفوظة عبر كل عمليات إعادة التشغيل.
6. لو شغّلت لوحة التحكم (Dashboard) أيضاً، تحتاج سيرفر Wispbyte ثانٍ منفصل بنفس الطريقة (Startup Command: `python3 dashboard/app.py`)، مع ضبط `BOT_API_BASE` ليشير لعنوان سيرفر البوت (استخدم الرابط العام لسيرفر البوت إن كان متاحاً، وإلا استضف الاثنين معاً وفعّل الاتصال الداخلي حسب دعم Wispbyte لذلك).

## صلاحيات Discord

فعّل Message Content Intent وServer Members Intent من Discord Developer Portal، وتأكد من أن دور البوت أعلى من الرتب التي سيديرها وأنه يملك Manage Messages وManage Roles وManage Channels وKick Members وBan Members وModerate Members حسب الأوامر المستخدمة.

## الربط مع الداشبورد

الداشبورد لا تحتاج إلى توكن Python مباشرة لتنفيذ أوامر بسيطة؛ بعد تشغيل البوت، تختار من صفحة الأوامر السيرفر والقناة، ثم ترسل الداشبورد الأمر إلى Discord عبر توكن البوت المحفوظ في أسرار الداشبورد. يجب أن يكون البوت موجوداً في السيرفر ويملك صلاحية قراءة القناة وإرسال الرسائل.

الأوامر المتقدمة التي تعتمد على قاعدة SQLite المحلية في Python تستمر في العمل داخل خدمة البوت، لكن قاعدة SQLite على طبقة مجانية ذات نظام ملفات مؤقت قد تفقد بياناتها عند إعادة التشغيل. للحفاظ على البيانات يلزم نقل التخزين إلى قاعدة خارجية دائمة أو استخدام خدمة بقرص دائم.

## المرجع

[1]: https://render.com/docs/free "Render Free Services Documentation"

## تحديث نظام اللفلات من حزمة Honor Pad 9

بعد تنزيل الحزمة الجديدة، أوقف العملية القديمة ثم فك الضغط فوق مجلد التشغيل مع خيار `-j` إذا كانت الملفات داخل مجلد فرعي. شغّل `python bot.py` من المجلد الذي يحتوي `bot.py` و`db.py` مع إبقاء `DISCORD_TOKEN` في ملف البيئة المحلي فقط. لا ترفع ملف `.env` ولا ترسله في المحادثة.

تدعم لوحة **نظام اللفلات** الآن أوامر `ضبط_خبرة_اللفل` و`ضبط_رسالة_اللفل`، إضافة إلى `ضبط_قناة_اللفلات` بقيم `حالية` أو `خاص` أو منشن قناة. المتغيرات المتاحة في الرسالة هي `{user}` و`{level}` و`{role}`، ويُستبدل `{role}` بقائمة الرتب التي مُنحت في عملية الارتقاء.

## تشغيل المشرف وإعادة التشغيل التلقائي

من داخل مجلد `spectre-bot-deploy` يمكن تشغيل المشرف بدلاً من تشغيل `bot.py` مباشرة:

```bash
chmod +x run-bot.sh
nohup ./run-bot.sh > supervisor.log 2>&1 &
echo $! > supervisor.pid
```

يتولى `run-bot.sh` تشغيل البوت، وتسجيل الخروج في `bot.log`، وإعادة تشغيله بعد 10 ثوانٍ إذا توقف. لا تشغّل `bot.py` و`run-bot.sh` معاً حتى لا تعمل نسختان من البوت بالتوكن نفسه.

استمرارية التشغيل بعد الخروج من Termux تعتمد على إعدادات بطارية Android؛ يجب اختيار `Unrestricted` لتطبيق Termux وإبقاء الجهاز متصلاً بالطاقة. إثبات الاستمرار الفعلي بعد الخروج وإعادة فتح التطبيق يجب التحقق منه على الجهاز نفسه.

## عبارات الحالة المتغيرة

يمكن تعديل العبارات من الملف `presence_config.py` داخل الحزمة، أو من ملف `.env` المحلي دون كشف التوكن. لاستخدام عبارات مخصصة، أضف إلى `.env`:

```env
SPECTRE_PRESENCE_MESSAGES=Spectre | حماية سيرفرك | التقديمات والتذاكر | اكتب /help للمساعدة
SPECTRE_PRESENCE_INTERVAL=60
```

تُفصل العبارات بعلامة `|`، وتُغيّر الحالة كل 60 ثانية. الحد الأدنى للفترة 15 ثانية، والحد الأعلى يوم واحد. يجب إعادة تشغيل البوت بعد تعديل `.env`. هذه الميزة تغيّر Presence الظاهر مثل `Playing`، لكنها لا تغيّر خانة Profile Prompt التي تديرها واجهة Discord لحسابات المستخدمين.

بعد تثبيت النسخة التي تحتوي على قاعدة Presence، يمكنك تعديلها من الداشبورد عبر صفحة **Presence البوت**؛ تحفظ اللوحة الأمر `ضبط_الحضور` في Discord، ويحفظ البوت العبارات في `bot_data.sqlite3` ويطبق أول عبارة فوراً. الصيغة اليدوية هي:

```text
!ضبط_الحضور 60 عبارة أولى | عبارة ثانية | عبارة ثالثة
```

لإعداد Counting استخدم `!اعداد_روم_العد #العد ✅ 60`، ولتصفير الرقم استخدم `!تصفير_العد #العد 0`، ولإيقاف روم استخدم `!حذف_روم_العد #العد`. يسمح النظام بأكثر من روم، ويبدأ الرقم التالي بعد الرقم المحفوظ. المخالفة الأولى تُحذف، والثانية تطبق Timeout بالمدة المحددة وسبب `عد بشكل متواصل لا تخرب`.

لتشغيل إذاعة القرآن، يكفي تثبيت FFmpeg ثم استخدام `!تشغيل_القرآن #روم-صوتي` أو زر التشغيل من صفحة Presence/التحكم في الداشبورد؛ النسخة الحالية تحتوي رابط HLS الرسمي كخيار افتراضي. يمكن وضع `QURAN_RADIO_STREAM_URL` في إعدادات الاستضافة إذا أردت استبدال الرابط، ويجب أن يكون رابط صوت مباشر مثل HLS وليس رابط صفحة ويب. للإيقاف استخدم `!ايقاف_القرآن`. يحتاج البوت إلى صلاحيات Connect وSpeak، وتستخدم العملية خيارات FFmpeg لإعادة الاتصال عند انقطاع البث.

## إصلاح رسالة «تعذر التعرف على أمر التحكم»

إذا ظهرت هذه الرسالة بعد الضغط على أحد أزرار اللفلات، فهذا يعني أن البوت الذي يستقبل رسالة الداشبورد يعمل بحزمة أقدم من كتالوج الداشبورد، وليس أن التوكن يحتاج إلى Reset. النسخة الجديدة تقبل صيغ أوامر اللفلات المفردة والجمع، وتطبع اسم الأمر غير المعروف للتشخيص.

لتحديث RunMy.App، أوقف الخدمة الحالية، ارفع الحزمة الأخيرة، فكها في مجلد التشغيل نفسه، ثم شغّل الخدمة من جديد. لا تغيّر `DISCORD_TOKEN` إذا كان البوت الظاهر في Discord هو نفسه؛ يجب أن تستخدم الداشبورد والبوت نفس حساب Discord، لكن تحديث الكود وحده لا يتطلب Reset للتوكن.

بعد إعادة التشغيل اختبر من Discord الأمر `!ضبط_خبرة_اللفل 15 25 60`، ثم جرّب زر **حفظ إعداد XP** من صفحة نظام اللفلات. إذا كان الأمر اليدوي يعمل والزر لا يعمل، تحقق من أن قناة تنفيذ الأوامر المختارة في الداشبورد هي قناة يستطيع البوت الكتابة والقراءة فيها.
